server:
  port: 8082

spring:
  application:
    name: cart-service
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://49.235.39.92:3306/hm-cart?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: mysql_zaPeJ8
  cloud:
    nacos:
      server-addr: 49.235.39.92:8848

mybatis-plus:
  configuration:
    default-enum-type-handler: com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler
  global-config:
    db-config:
      update-strategy: not_null
      id-type: auto


logging:
  level:
    com.hmall: debug
    com.hmall.api.client: debug
  pattern:
    dateformat: HH:mm:ss:SSS
  file:
    path: "logs/${spring.application.name}"

knife4j:
  enable: false
  openapi:
    title: 黑马商城购物车服务接口文档
    description: "黑马商城购物车服务接口文档"
    email: itheima@itcast.cn
    concat: itheima
    url: https://www.itcast.cn
    version: v1.0.0
    group:
      default:
        group-name: default
        api-rule: package
        api-rule-resources:
          - com.hmall.cart.controller
feign:
  okhttp:
    enabled: true # 你开启了 OKHttp，这很好
  client:
    config:
      default: # 这里配置全局默认规则
        loggerLevel: FULL # 🚨 强制设置全局日志级别为 FULL

